import { Address123Directive } from './address123.directive';

describe('Address123Directive', () => {
  it('should create an instance', () => {
    const directive = new Address123Directive();
    expect(directive).toBeTruthy();
  });
});
